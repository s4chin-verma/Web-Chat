import { register, login ,getAllUsers } from './user.controller';
import { resetPassword, forgotPassword } from './password.controller';

export { register, login, resetPassword, forgotPassword, getAllUsers };
